# Wildcards Quiz

## Example 1

```sql
SELECT * from users
    WHERE name LIKE 'or_%';
```

## Example 2

```sql
SELECT * from users
    WHERE name LIKE '__ing';
```

You can find out what this tow examples do... have fun!