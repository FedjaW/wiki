```js
const primes = [2, 3, 5, 7, 11]

console.log(typeof primes) // => obejct

// es gibt keinen speziellen Typ für array, daher wird object zurück gegeben
// arrays sind quasi eine Sonderform von Object
// früher waren arrays mal mittel objects implementiert, das hat sich aber geändert
```
